@page
@model CBTInformationSystemWeb.Pages.Account.ChangeUserPasswordModel
@{
    ViewData["Title"] = "Change Student Password";
}
<div class="layout-px-spacing">
    <div class="row layout-top-spacing" id="cancel-row">
        <div class="col-xl-6 col-lg-6 col-sm-6 offset-sm-3 layout-spacing">
            <div class="card mt-lg-5">
                <div class="card-body">
                    <div class="h4 text-center text-dark">CHANGE STUDENT PASSWORD</div>
                    @if (TempData["error"] != null)
                    {
                        <div class="alert alert-danger mt-2" role="alert">
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close">&times;</button>
                            <strong><i class="mdi mdi-checkbox-marked-circle-outline display-4 text-danger"></i></strong> &emsp; @TempData["error"].
                        </div>
                    }
                    <form method="post">
                        <div class="form-group mb-4">
                            <label for="formGroupExampleInput">Username/Email</label>
                            <input disabled class="form-control" asp-for="Username" placeholder="Example input">
                        </div>
                        <div class="form-group mb-4">
                            <label for="formGroupExampleInput2">Password</label>
                            <input type="text" class="form-control" name="password" id="password" placeholder="Password" required>
                        </div>
                        <button type="submit" name="time" class="btn btn-primary">
                            Change Password
                        </button>
                        <input type="hidden" asp-for="Code" />
                        <input type="hidden" asp-for="Username" />
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
